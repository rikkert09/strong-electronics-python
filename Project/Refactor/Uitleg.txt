style.use('fivethirtyeight')
fig = plt.figure()
ax1 = fig.add_subplot(111)


def animate(i):
    graph_data = open('graphdata.txt', 'r').read()
    lines = graph_data.split('\n')
    xs = []
    ys = []
    for line in lines:
        if len(line) > 1:
            x, y = line.split(',')
            xs.append(int(x))
            ys.append(int(y))
    ax1.clear()
    ax1.plot(xs, ys)

is constant dus moet voor aan staan in de Zengmain
maar dit wil jij aangeroepen zien worden op de page van ControlUnit bij de graph plek dat is onderaan in die file

en dit moet weer in de main gedraaid worden

if __name__ == "__main__":
    ani = animation.FuncAnimation(fig, animate, interval=1000)
	
dit staat nog in controlGUI
        canvas = FigureCanvasTkAgg(fig, self)
        canvas.show()
        canvas.get_tk_widget().grid(row=0, column=2, rowspan=10)
	vanwege dat de positie op het 2de frame is vandaar de positie enzo
	
	video : https://www.youtube.com/watch?v=JQ7QP5rPvjU&t=562s